"""Shared redesign component library (DEC-208).

Reusable, token-driven visual components consumed by the new global shell and by
each later per-page migration: cards, badges/pills, buttons, section headers,
dense tables, a modal-dialog base, and the glow/LED + animation-controller
primitives. Import from the submodules directly.
"""
